import styles from '../styles/main.module.css'

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <h3 className={styles.footer_header}>NestAuthAPI</h3>
    </footer>
  );
};

export default Footer;
